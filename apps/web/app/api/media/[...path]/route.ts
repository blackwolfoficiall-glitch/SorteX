import{authenticatedApiFetch,jsonFromUpstream,readJson}from'@/lib/auth/server';async function proxy(request:Request,{params}:{params:Promise<{path:string[]}>}){const{path}=await params;const query=new URL(request.url).search;const body=['GET','HEAD'].includes(request.method)?undefined:await request.text();const response=await authenticatedApiFetch(`/media/${path.join('/')}${query}`,{method:request.method,body:body||undefined,headers:body?{'Content-Type':'application/json'}:undefined});if(response.headers.get('content-type')?.includes('image/'))return new Response(response.body,{status:response.status,headers:{'Content-Type':response.headers.get('content-type')!}});return jsonFromUpstream(await readJson(response),response.status)}export const GET=proxy;export const POST=proxy;export const PATCH=proxy;export const DELETE=proxy;
