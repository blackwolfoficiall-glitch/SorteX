"use client";import{shareMedia}from'@/lib/media/client';export default function ShareCenter({id,title}:{id:string;title:string}){async function share(channel:string){const link=await shareMedia(id,channel);const text=`${title} — confira na SorteX: ${link.publicUrl}`;if(channel==='NATIVE_SHARE'&&navigator.share)await navigator.share({title,text,url:link.publicUrl});else if(channel==='WHATSAPP')window.open(`https://wa.me/?text=${encodeURIComponent(text)}`,'_blank','noopener,noreferrer');else if(channel==='TELEGRAM')window.open(`https://t.me/share/url?url=${encodeURIComponent(link.publicUrl)}&text=${encodeURIComponent(title)}`,'_blank','noopener,noreferrer');else await navigator.clipboard.writeText(text)}return <div className="mt-3 flex flex-wrap gap-2">{[['WHATSAPP','WhatsApp'],['TELEGRAM','Telegram'],['COPY_LINK','Copiar link'],['NATIVE_SHARE','Compartilhar']].map(([c,l])=><button key={c} onClick={()=>share(c)} className="rounded-xl bg-violet-50 px-3 py-2 text-xs font-bold text-violet-700">{l}</button>)}</div>}
